/// <reference types="vite/client" />

// Declares the side-effect import of `./styles.css` (and Vite's other asset
// modules). TypeScript 7 rejects importing a module it has no declarations for,
// where 5.x let it pass silently.
